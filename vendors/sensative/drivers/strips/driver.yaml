# Formal driver name
name: Sensative strips driver
# Simple description of the driver functionalities
description: Decoder for Sensative Strips Presence
# Company that developed the driver
developedBy: actility
# Email of the developer
developerEmail: lars.mats@sensative.com
# In case of a driver per model: `<vendorId>:<modelId>:<modelVersion>`
# In case of a driver for multiples model: `<vendorId>:generic:<genericVersion>`
# Needed for linking the model with a specific driver -> must be the same one used in model.yaml in the corresponding model
protocolId: sensativ:strips:1
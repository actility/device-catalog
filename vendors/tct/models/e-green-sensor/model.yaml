# Commercial name of the model
name: e-green sensor
# Functional description of the product. Maximum 500 characters.
description: Self-powered clip-on current sensor with no battery, harvesting its energy from the monitored cable. Measures 0-200 A RMS on one phase and the voltage of its internal supercapacitor, plus temperature with an optional type K thermocouple. LoRaWAN and BLE. Also sold by ATIM as ACW/LW8-CTS (with thermocouple) and ACW/LW8-CS (without probe).
# Logo of the device
logo: tct_e-green-sensor.png
# ID(s) of the profile that defines the LoRaWAN characteristics of this model.
# id: as defined under /profiles.
# lorawanDeviceProfileID: as defined by LoRa Alliance in https://read.uberflip.com/i/1507778-lora-alliance-vendor-id-apr2026/.
# The lorawanDeviceProfileID consists of 8 upper-case hexadecimal characters, 4 characters for VendorID + 4 characters for VendorProfileID. Please consider using single (') or double (") quotation.
deviceProfileIds:
  - id: tct_RFGroup1_1.0.4_classA
    lorawanDeviceProfileID:

# You may optionally customize any of the following settings to override the generic value set in LoRaWAN device profiles associated with your model. Leave empty if you want to keep the Device Profile settings.
# Maximum device TX Conducted output power in dBm.
maxTxPower:
# Minimum device TX Conducted output power in dBm.
minTxPower:
# Maximum device TX Radiated output power in dBm.
maxTxEIRP:
# Minimum device TX Radiated output power in dBm.
minTxEIRP:
# Typical mobility profile of the device. Possible values are 'near_static' (also valid for static devices), 'walking_speed', 'vehicular_speed'
# or 'random' (not known, changes over time).
motionIndicator:

# Is your device certified by the LoRa Alliance? Possible values: true, false.
LoRaWANCertified: false
# Always mandatory: `<vendorId>:<modelName>:<modelVersion>` (You should have only one per model)
# Format: `<8 characters max>:<16 characters max>:<digits only>`
# Be careful: Once you chose a model, it cannot be changed later
modelId: tct:e-green-sensor:1
# <vendorId>:<protocolName>:<protocolVersion> Needed for linking the model with a specific driver -> must be the same one used in driver.yaml in the corresponding model (You might have several ones)
# Format: `<8 characters max>:<16 characters max>:<digits only>`
# Be careful: Once you chose a protocolId, it cannot be changed later
protocolId:
  - tct:e-green-sensor:1
# Informative, maintained by Netmore: true when the driver of the first protocolId above ships a
# BACnet/Modbus mapping (mapping.json, in Netmore's private catalog), so that the device can be
# exposed through the BACnet and Modbus gateways. Checked: true requires that mapping.
supportedBACnetModbus: false
# DataSheet URL (optional)
specificationURL: https://www.tct.fr/en/toPDF/26
# User Guide URL (optional)
userGuideURL: https://www.tct.fr/uploads/kcfinder/files/E%20green%20Sensor%20user%20guide(1).pdf
# <sensor>:<unitId> Available sensors following Actility ontology: https://github.com/actility/device-catalog/blob/main/template/sample-vendor/drivers/ONTOLOGY.md
sensors:
  - current:A
  - voltage:V
  - temperature:Cel
# Driver examples
# A list of examples description that are compatible with this model
examples:
  tct:e-green-sensor:1:
    - "Measurement: current and voltage, 1 sample"
    - "Measurement: current and voltage, 2 samples"
    - "Measurement: temperature, current and voltage, 1 sample"
    - "Measurement: temperature, current and voltage, 2 samples"
    - "Alert: voltage below the low threshold"
    - "Configuration acknowledgment: all parameters applied"
LoRaWANRelay:
  canOperateAsARelay: false
  defaultRelayActivation: false
